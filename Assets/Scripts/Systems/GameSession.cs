/// <summary>
/// Статический класс для передачи выбранного вида между сценами.
/// Живёт в памяти всё время пока запущено приложение.
/// Никаких Update(), никакой проверки каждый кадр — просто ячейка данных.
/// </summary>
public static class GameSession
{
    /// <summary>
    /// Выбранный вид динозавра. Задаётся при выборе в меню.
    /// Читается один раз при старте игровой сцены.
    /// </summary>
    public static DinosaurSpeciesData SelectedSpecies;
}
